# Restaurant App 🍽️

A modern React restaurant application with:
- Offcanvas Cart (Cartzilla-style)
- Geolocation-based delivery suggestions
- Responsive Navbar & Footer
- Vite + React

## Run locally
```bash
npm install
npm run dev

Then push:

```bash
git add README.md
git commit -m "Add README"
git push
